# Neon Postgres Database URL (Get this from your Neon Console)
DATABASE_URL=postgres://username:password@hostname.neon.tech/neondb?sslmode=require

# Vercel Blob Token (Required only if testing Blob uploads locally, otherwise auto-injected on Vercel)
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...
